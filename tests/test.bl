/* playground */

#load "std/print.bl"

main :: fn () s32 {
    defer print("Hello");
    return 0;
}



// Use #comptime as more generic flag for static if and fucntion calls in compile time???
/*
    1) Defer generation of AST to MIR
    
    2) Generate MIR but insert only used tmporary block of branch.

    3) Generate regular if branches but ignore on of them.
 */